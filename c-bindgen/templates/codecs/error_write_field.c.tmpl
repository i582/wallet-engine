            status = wallet_engine_private_write_{{FIELD_CODEC_NAME}}(
                writer,
                value.payload.{{PAYLOAD_MEMBER_NAME}}.{{FIELD_C_NAME}}
            );
            if (status != WALLET_ENGINE_ABI_STATUS_OK) {
                return status;
            }
