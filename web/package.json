{
  "name": "@ton/wallet-engine",
  "version": "0.0.4",
  "private": true,
  "license": "MIT OR Apache-2.0",
  "author": "TON Core",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "bindings": "cargo xtask bindings wasm",
    "build": "tsc --noEmit",
    "fmt": "biome format --write .",
    "fmt:check": "biome format .",
    "lint": "biome check --formatter-enabled=false .",
    "lint:fix": "biome check --formatter-enabled=false --write .",
    "precommit": "bun fmt && bun lint && bun build && bun test",
    "test": "bun test test"
  },
  "dependencies": {
    "@tonconnect/protocol": "3.0.0",
    "idb": "8.0.3"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.0",
    "bun-types": "^1.3.14",
    "typescript": "^7.0.2"
  },
  "packageManager": "bun@1.3.14"
}
